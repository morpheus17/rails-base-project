<div class="container mt-5">
  <div class="my-5">
    <h1 class="text-center fw-bold pt-5">Transactions</h1>
  </div>
  <div id ="stock-searched" class="d-flex justify-content-center row">
    <% if @transactions.length > 0 %>
      <%= render partial: "transactions/transaction_list" %>
    <% else %>
      <p class="text-muted text-center my-5">No activities yet.</p>
    <% end %>
  </div>
</div>