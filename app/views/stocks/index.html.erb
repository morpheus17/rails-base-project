<div class="container">
  <div class="">
    <h1 class="text-center my-5 fw-bold">My Portfolio</h1>
  </div>
  <div id ="stock-searched" class="d-flex justify-content-center row">
    <% if @stocks.length > 0 %>
      <%= render partial: "stocks/stock_list" %>
    <% else %>
      <p class="text-muted text-center my-5">Nothing to show. Go to Market and search for stocks.</p>
    <% end %>
  </div>
</div>